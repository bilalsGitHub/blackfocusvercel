"use client";

// Auto-start logic is now handled directly in timer-store's completeTimer function
// This hook is kept for backwards compatibility but is no longer needed
export function useAutoStart() {
  // No-op - logic moved to timer-store
}
